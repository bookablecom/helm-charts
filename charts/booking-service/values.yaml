spring-boot:
  nameOverride: "booking-service"
  fullnameOverride: "booking-service"
  image:
    repository: ghcr.io/bookablecom/booking-service
    tag: 0.0.19
  imagePullSecrets:
    - name: "dockerconfigjson-github-com"
  extraEnv:
    CONFIG_SERVER_URI: http://config-server:8080
  ingress:
    enabled: true
    match: Host(`api.bsgrd.com`) && PathPrefix(`/bookings`)
  name: booking-service
  healthChecks:
    enabled: true
    readinessProbe:
      initialDelaySeconds: 120
      periodSeconds: 40
      timeoutSeconds: 15
      failureThreshold: 60
    livenessProbe:
      initialDelaySeconds: 120
      periodSeconds: 30
      timeoutSeconds: 15
      failureThreshold: 60
  replicaCount: 1
  spring:
    profiles:
      active: default
traefik-ingress:
  ingress:
    match: "Host(`api.bsgrd.com`) && PathPrefix(`/bookings`)"
    tls:
      enabled: true
      resolver: "letsencrypt"
  service:
    name: "booking-service"
    port: 8080
