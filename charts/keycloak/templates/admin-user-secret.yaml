apiVersion: v1
kind: Secret
metadata:
  name: keycloak-admin-user-secret
  annotations:
    kubernetes.io/service-account.name: {{ .Values.name }}
type: "Opaque"
stringData:
  KEYCLOAK_USER: {{ .Values.admin.username }}
  KEYCLOAK_PASSWORD: {{ .Values.admin.password }}