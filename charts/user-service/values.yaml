replicaCount: 1

image:
  repository: ghcr.io/bookablecom/user-service
  pullPolicy: Always
  tag: "1.3-SNAPSHOT"

fullnameOverride: "user-service"

serviceAccount:
  create: true

service:
  type: ClusterIP
  port: 8080