replicaCount: 1

image: ghcr.io/bookablecom/config-server:2.2.2

fullnameOverride: "config-server"
name: "config-server"

spring:
  profiles:
    active: vault,git

mesh:
  enabled: true

serviceAccount:
  create: true

service:
  type: ClusterIP
  port: 8080

configserver:
  repo_uri: https://github.com/bookablecom/configurations

github:
  username: ""
  password: ""

vault:
  host: vault.vault.svc.cluster.local
  token: ""