replicaCount: 1

image: ghcr.io/bookablecom/config-server:2.0.3

fullnameOverride: "config-server"
name: "config-server"

mesh:
  enabled: true

serviceAccount:
  create: true

service:
  type: ClusterIP
  port: 8080

configserver:
  repo_uri: https://github.com/bookablecom/configurations

github:
  username: ""
  password: ""

vault:
  host: vault.default.svc.cluster.local
  token: ""